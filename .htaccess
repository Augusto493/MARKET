# Hostinger / shared hosting: document root = repo root (public_html)
# Redireciona todas as requisições para a pasta public do Laravel
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
